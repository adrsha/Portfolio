---
import styles from './MarkedSection.module.css'

const {mark, title, ...extraAttrs} = Astro.props;
---
<section class={styles.markedSection} {...extraAttrs}>
    { title && <h1 class:list={[styles.sectionHeader, styles.header1]}>{title}</h1> }
    <slot />
</section>
